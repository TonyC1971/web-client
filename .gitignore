# Generated by publish-minimal. It covers what a CLONE produces, nothing the snapshot ships.

# 🚨 EVERY PATTERN IS ROOT-ANCHORED. An unanchored `data/` matches a directory named data at
# ANY depth, and this tree has source/*/src/ClassicUO.Client/Game/Data — 95 source files that
# vanished from the commit while the publisher reported writing them.
/node_modules/
/server/dist/

# Built client bundle and the game files you supply.
/client/
/gamefiles/

# Your install: secrets, your shard, its database.
/.env
/data/
/servers/*.yaml
!/servers/*.yaml.example
